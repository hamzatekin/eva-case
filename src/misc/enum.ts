export enum EvaApiEndpoint {
    LOGIN = '/user/auth',
    SALES_FINANCE = '/data/sales-finances',
    SALES_AND_EXPENSE_BY_REQUEST_DATE = '/data//sales-expense-by-request-date',
}

export enum AppRoutes {
    LOGIN = '/login',
    HOME = '/',
}

export enum AppRouteNames {
    LOGIN = 'login',
    HOME = 'home',
}
